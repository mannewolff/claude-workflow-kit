#!/usr/bin/env node
// Platzhalter — wird in Issue #4 implementiert.
// Kopiert die acht Skills nach ~/.claude/skills/ oder ./.claude/skills/
// und schreibt .claude/workflow.config.json aus fünf interaktiven Fragen.
console.error("install.mjs ist noch nicht implementiert (Issue #4).");
process.exit(1);
