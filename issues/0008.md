---
id: 0008
type: task
status: in_review
title: board.mjs: backlog-Behandlung config-getrieben machen (createIssue/moveIssue folgen dem Mapping)
created: 2026-07-07
---
## Kontext

Der GitLab-Adapter behandelt die Backlog-Spalte an zwei Stellen fest verdrahtet, statt dem Config-Mapping columns.backlog zu folgen. Dadurch widersprechen sich die Operationen, sobald ein Projekt backlog als echtes Label fuehrt (Variante A: columns.backlog = \"Backlog\", Board hat eine label-basierte "Backlog"-Liste):


        
      
createIssue() setzt hart glab issue update <id> --label \"Backlog\" (aktuell Zeile 479) und ignoriert das Mapping.

        
      
moveIssue() behandelt to === \"backlog\" in der Open/Closed-Sonderbehandlung (aktuell Zeile 525): es entfernt alle Status-Labels und reopnet, setzt also kein Backlog-Label. Ein Move nach backlog wirft das Issue damit aus der Backlog-Spalte.

Folge: Anlegen landet zufaellig richtig (weil createIssue "Backlog" hartkodiert), aber move <id> backlog und list --status backlog passen nicht dazu. createIssue, moveIssue, listIssues und labelToStatus muessen dieselbe Quelle der Wahrheit benutzen: das Config-Mapping.
Vorgeschichte: Bugs #2 (list --state), #3 (createIssue Phantom-Label) und #4 (moveIssue verliert Ziel-Label) wurden bereits adressiert; dieses Issue schliesst die verbliebene Inkonsistenz bei der backlog-Spalte.
## Aufgabe



        
      Datei: .claude/kit/board.mjs, Klasse GitLabIssueTracker.

        
      
createIssue(): das hartkodierte --label \"Backlog\" durch columnLabels(cfg).backlog ersetzen. Sonderfall beachten: mappt backlog auf den nativen Open-Zustand (kein echtes Label), darf kein Label gesetzt werden — dann Issue einfach offen lassen.

        
      
moveIssue(): \"backlog\" aus der Open/Closed-Sonderbehandlung herausnehmen, wenn backlog per Config ein echtes Label ist, sodass backlog den normalen Label-Pfad laeuft (Ziel-Label setzen, andere Status-Labels entfernen — Ziel-Label nicht im selben Aufruf unlabeln, vgl. #4). \"done\" bleibt als Closed-Zustand.

        
      Entscheidungskriterium einheitlich definieren: Wann ist eine Spalte ein GitLab-Zustand (Open/Closed) und wann ein Label? Vorschlag: Zustaende sind ausschliesslich Open (backlog, falls so gemappt) und Closed (done); alle anderen Mappings sind Labels. Diese Regel an einer Stelle kapseln und von create/move/list/labelToStatus nutzen.

        
      Falls das Kit eine Default-/Template-workflow.config.json ausliefert: deren columns.backlog-Default konsistent zur gewaehlten Semantik setzen und dokumentieren.

## Akzeptanzkriterium



        
      Mit columns.backlog = \"Backlog\": issue create legt ein Issue mit Label "Backlog" an, das in der Backlog-Spalte erscheint; issue move <id> backlog setzt/behaelt das Label "Backlog"; issue list --status backlog findet es; labelToStatus gibt backlog zurueck.

        
      Mit columns.backlog = \"Open\" (Open-Zustand): issue create legt ein offenes Issue ohne Status-Label an; issue move <id> backlog entfernt die Status-Labels und laesst das Issue offen; kein Phantom-Label "Open".

        
      
issue move <id> done schliesst das Issue in beiden Varianten (kein Phantom-Label "Closed").

        
      Kein Vorkommen von hartkodiertem "Backlog" mehr im GitLab-Adapter.

## Abhaengigkeiten

Keine harten. Gleicher Adapter und thematisch verzahnt mit #0007 und #0006; sinnvollerweise zusammen mit diesen umsetzen.


---
**Kommentar** (2026-07-07 16:54)

## Abschlussbericht Issue #0008

### Änderungen
- `kit/board.mjs`:
  - Neue zentrale Funktion `isStateColumn(status, config)`: `done` ist immer GitLab-Zustand Closed; `backlog` ist GitLab-Zustand Open nur bei `columns.backlog === \"Open\"` (Sonderwert), sonst normales Label. Einzige Quelle der Wahrheit für alle vier betroffenen Funktionen.
  - `createIssue()`: hartkodiertes `--label \"Backlog\"` ersetzt durch `columnLabels(cfg).backlog`, übersprungen wenn backlog als Open-Zustand konfiguriert ist.
  - `moveIssue()`: Bedingung von `to === \"backlog\" || to === \"done\"` auf `isStateColumn(to, cfg)` umgestellt — backlog läuft jetzt (außer im Open-Sonderfall) über den normalen Label-Pfad.
  - `listIssues(status)`: für Zustandsspalten jetzt `--closed` (done) bzw. `--not-label` je anderem Status-Label (backlog-als-Open), sonst unverändert `--label`.
  - `labelToStatus(labelNames, config, state)`: neuer dritter Parameter `state`; Zustandsspalten werden aus der Label-Schleife ausgenommen und stattdessen über `state === \"closed\"` → done bzw. `state === \"opened\"` + Open-Konfiguration → backlog aufgelöst. `getIssue`/`listIssues` übergeben jetzt den Issue-Zustand mit.
- `templates/workflow.config.schema.json` — Beschreibung von `columns.backlog`/`columns.done` um die GitLab-Sonderfälle (Open-Sentinel, done immer Closed) ergänzt.
- `install.mjs` — eingebetteter BOARD_MJS_B64-Blob neu synchronisiert.

### Tests und Checks
- `node --check kit/board.mjs` → Syntax OK.
- Kein hartkodiertes Label-Literal \"Backlog\" mehr im GitLab-Adapter (grep-verifiziert, nur noch als Default-Config-Wert und in Kommentaren/Meldungen).
- End-to-End gegen Fake-`glab` (Mehrfach-Issue, inkl. echtem Label-Netting- und Filter-Verhalten), **Variante A** (`columns.backlog = \"Backlog\"`, Default): create → Label gesetzt; `list --status backlog` findet es; `get` liefert Status backlog; `move backlog` (nach vorherigem `move in_progress`) setzt Label \"Backlog\" korrekt neu (der eigentliche Bug ist damit behoben); `move done` schließt ohne Phantom-Label. Alle grün.
- **Variante B** (`columns.backlog = \"Open\"`): create → offenes Issue ohne Status-Label; `move ready` dann `move backlog` → Labels entfernt, offen, kein Phantom-Label \"Open\"; `get` löst Status backlog über den Zustand auf; `move done` schließt ohne Phantom-Label; zusätzlich `list --status backlog`/`--status done` liefern in dieser Variante ebenfalls die korrekten Issues (über `--not-label`/`--closed`), obwohl das über die Akzeptanzkriterien hinausgeht (im Aufgabentext aber explizit gefordert).
- Reale lokal installierte `glab 1.107.0`: alle neu erzeugten Kommandozeilen (`--not-label` mehrfach, `--closed`, Label/Unlabel-Kombination) syntaktisch validiert (nur Repo-Fehler außerhalb eines echten Projekts, keine Flag-Fehler).
- `node tools/sync-blobs.mjs --check` → Blobs synchron.
- `node -e \"JSON.parse(...)\"` → Schema bleibt valides JSON.

### Hinweise
- Phantome-Label-Altlasten (z.B. ein vor #0007 versehentlich gesetztes Label \"Done\" auf einem noch offenen Issue) werden von `list --status backlog` in Variante B nicht herausgefiltert — das liegt außerhalb der Akzeptanzkriterien und wurde bewusst nicht zusätzlich behandelt, um den Scope nicht auszuweiten.
- Kein Zugriff auf ein echtes GitLab-Projekt; Verifikation erfolgte end-to-end gegen ein Fake-`glab` plus Flag-Validierung gegen die echte lokal installierte Binary.