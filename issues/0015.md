---
id: 0015
type: task
status: in_review
title: docs/dokumentation.md: Toolbox-Adapter als privates Setup dokumentieren
created: 2026-07-09
---
## Kontext
Nach dem Upstreamen des Toolbox-Adapters (Issue #0013) und der Schema-Ergaenzung (Issue #0014) fehlt noch die Prosa-Dokumentation auf docs.mwolff.org (`docs/dokumentation.md`). Der Abschnitt "## Issue-Tracker und Code-Host" (ab Zeile ~294) dokumentiert aktuell github/gitlab/local. Toolbox soll ergaenzt werden, aber explizit als "Hidden Feature" gerahmt: ein persoenliches Setup des Autors (eigenes Kanban-Tool, Java/Spring-Boot-Backend + React-Frontend), kein oeffentlich beworbenes Kit-Feature, kein Installer-Dialog dafuer. Ton mit Manne am 2026-07-09 abgestimmt: neutral, erklaerend, nicht rechtfertigend oder seltsam wirkend — primaer zum eigenen Nachschlagen gedacht.

## Aufgabe
In `docs/dokumentation.md`, neue Unterueberschrift `### Toolbox (privates Setup)` nach dem bestehenden Abschnitt "### Konfiguration einstellen" (~Zeile 368), innerhalb von "## Issue-Tracker und Code-Host":
- Hinweis-Absatz zu Beginn: kein oeffentlich beworbenes Feature, persoenliches Kanban-Tool des Autors, nicht Teil des Installer-Dialogs, hier dokumentiert zum eigenen Nachschlagen.
- Config-Beispiel: `{ "issueTracker": "toolbox", "toolbox": { "host": "https://toolbox.mwolff.org" }, "codeHost": "github" }` — mit Hinweis, dass `codeHost` unabhaengig bleibt (PRs weiterhin ueber GitHub/GitLab, Toolbox ist nur Issue-Tracker, kein Code-Host).
- Auth-Weg: Kanban-Access-Token (PAT), erzeugt in der Toolbox-Web-UI, per `tbx auth login` gespeichert unter `~/.config/toolbox-cli/{config,tokens}.json`, Header `X-Kanban-Token`, wirkt nur auf `/api/kanban/**`.
- Hinweis: Spaltennamen sind bei Toolbox fix (BACKLOG/READY/IN_PROGRESS/IN_REVIEW/DONE), anders als bei GitHub/GitLab NICHT ueber `columns` in der Config veraenderbar.
- Hinweis: Einrichtung des `tbx`-CLI und Token-Erzeugung sind Teil des Toolbox-Projekts selbst, nicht dieses Kits — Verweis auf die Toolbox-README fuer Details.

## Akzeptanzkriterium
- Neuer Abschnitt ist vorhanden, an der beschriebenen Stelle eingefuegt, Markdown rendert fehlerfrei (keine kaputten Codebloecke, konsistente Ueberschriften-Ebene ###).
- Inhaltlich deckungsgleich mit dem tatsaechlich in Issue #0013 upgestreamten Adapter-Verhalten (Config-Feldnamen, Auth-Mechanismus, fixe Spalten).
- Ton entspricht der mit Manne abgestimmten Rahmung (neutral, nicht werblich, klar als privates Setup gekennzeichnet).

## Abhängigkeiten
Issue #0013 muss vorher fertig sein (Doku beschreibt den tatsaechlich gelieferten Code).

---
**Kommentar** (2026-07-09 09:33)

## Abschlussbericht Issue #0015

### Änderungen
- `docs/dokumentation.md` — neuer Abschnitt "### Toolbox (privates Setup)" nach "### Konfiguration einstellen" im Kapitel "## Issue-Tracker und Code-Host": Einordnung als privates Setup, Config-Beispiel, PAT-Auth-Weg, Hinweis auf fixe Spaltennamen, Verweis auf das Toolbox-Projekt fuer CLI/Token-Verwaltung.

### Tests und Checks
- Manuell geprueft: Ueberschriften-Ebene konsistent (### innerhalb der bestehenden ##-Sektion), keine kaputten Codebloecke
- Inhaltlich gegen den in Issue #0013 upgestreamten Adapter-Code abgeglichen (Config-Feldnamen `issueTracker`/`toolbox.host`, Auth-Header `X-Kanban-Token`, fixe Status-Spalten-Zuordnung)

### Hinweise
- Ton wie mit Manne am 2026-07-09 abgestimmt: zurueckhaltend, Nachschlage-Charakter, kein beworbenes Feature.