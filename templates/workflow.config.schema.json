{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "claude-workflow-kit workflow.config",
  "description": "Projekt-spezifische Konfiguration für die claude-workflow-kit-Skill-Bibliothek. Liegt als .claude/workflow.config.json im Repository und gilt teamweit. Persönliche Abweichungen gehören in .claude/workflow.config.local.json (nicht im Repository); daraus gewinnen nur reviewModel, reviewCommand, reviewScope, triggers und toolbox.tokenFile, alles andere wird ignoriert und gemeldet. Setzt die persönliche Datei eines der beiden Reviewer-Felder, weicht das andere aus der geteilten Config — die Oder-Regel gilt auch nach dem Merge.",
  "type": "object",
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Versionskennung x.y.z. push main erhöht z, merge production erhöht y, x nur explizit. Wird auf dem Board angezeigt. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": "1.4.0"
    },
    "codeHost": {
      "type": "string",
      "enum": [
        "github",
        "gitlab",
        "local"
      ],
      "description": "Wo der Code liegt (Push, Pull Requests). Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": "github"
    },
    "issueTracker": {
      "type": "string",
      "enum": [
        "github",
        "gitlab",
        "local",
        "toolbox"
      ],
      "description": "Wo die Issues verwaltet werden. Darf vom codeHost abweichen. 'toolbox' ist ein privates Setup (eigenes Kanban-Tool des Autors), nicht Teil des Installer-Dialogs und nur per manueller Config-Bearbeitung nutzbar. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": "github"
    },
    "provider": {
      "type": "string",
      "enum": [
        "github",
        "gitlab",
        "local"
      ],
      "description": "Veraltet (v1). Wird beim Laden auf codeHost/issueTracker migriert. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert."
    },
    "buildChecks": {
      "type": "array",
      "items": {
        "description": "Ein Eintrag hat eine von drei Formen. (1) Der bloße Kommandostring \"npx eslint .\": nicht zugeordnet, läuft immer. (2) { \"cmd\": \"...\", \"areas\": [\"backend\"] }: läuft, wenn einer der genannten Bereiche berührt ist; die Bereichsnamen stehen in checkAreas. (3) { \"cmd\": \"...\", \"always\": true }: entschieden immer laufend. Form 1 und Form 3 verhalten sich gleich, bedeuten aber Verschiedenes — vergessen gegen entschieden. Ein Objekt nur mit \"cmd\" bedeutet dasselbe wie die String-Form. \"areas\" und \"always\" schließen sich aus (eine Vorrangregel würde niemand lesen), und \"areas\" braucht mindestens einen Eintrag (ein leeres Array liefe nie).",
        "oneOf": [
          {
            "type": "string"
          },
          {
            "type": "object",
            "properties": {
              "cmd": {
                "type": "string",
                "minLength": 1,
                "description": "Die Kommandozeile, wie sie in der String-Form stünde."
              },
              "areas": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "description": "Bereichsnamen aus checkAreas. Die Prüfung läuft, wenn mindestens einer der Bereiche berührt ist. Nicht zusammen mit \"always\"."
              },
              "always": {
                "type": "boolean",
                "description": "true = entschieden immer laufend, unabhängig von den berührten Bereichen. Nicht zusammen mit \"areas\"."
              }
            },
            "required": [
              "cmd"
            ],
            "additionalProperties": false,
            "not": {
              "required": [
                "areas",
                "always"
              ]
            }
          }
        ]
      },
      "description": "Kommandos, die /local-check sequenziell ausführt (Build, Tests). Leer-Array = keine automatisierten Checks. Ein Eintrag ist entweder ein Kommandostring oder ein Objekt mit Bereichszuordnung (siehe items und checkAreas). Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": []
    },
    "checkAreas": {
      "type": "object",
      "description": "Benannte Bereiche des Projekts: Schlüssel ist der Bereichsname, Wert eine Liste von Pfadmustern. Auf diese Namen zeigt \"areas\" in der Objektform eines buildChecks-Eintrags. Im Zusammenspiel der drei Formen: ein bloßer Kommandostring läuft immer (nicht zugeordnet), { \"cmd\", \"areas\" } läuft nur, wenn eines der hier hinterlegten Muster berührt ist, { \"cmd\", \"always\": true } läuft entschieden immer — String und always:true verhalten sich gleich, bedeuten aber Verschiedenes (vergessen gegen entschieden). Ein Bereich ohne Muster erfasst nichts. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "spec": {
      "type": "object",
      "description": "Schalter für Spec-Driven Development — die Spezifikation des fachlichen Soll-Verhaltens unter specs/. Das Vorhandensein des Blocks bedeutet eingeschaltet — es gibt bewusst kein Feld 'enabled', denn ein Bool hätte einen Aus-Zustand, und die Entscheidung ist nicht zurückzunehmen: Es gibt keinen Weg zurück. Der Zeitpunkt steht in 'seit'; nur Pakete mit einem Anlagedatum ab diesem Tag wertet das spätere Gate. ACHTUNG: Der Block trägt nicht auf jedem Tracker. Bei issueTracker github und gitlab weist spec.mjs jeden Lauf ab — dort gibt es weder Aktivitätsverlauf noch Suche über Aussagen, auf denen Spec-Driven Development aufsetzt. Möglich sind toolbox und local. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "seit": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Ab wann die Spezifikation gilt (JJJJ-MM-TT). Nur Pakete mit einem Anlagedatum ab diesem Kalendertag wertet das spätere Gate; ältere bleiben unberührt."
        },
        "bereiche": {
          "type": "object",
          "minProperties": 1,
          "description": "Bereichsnamen auf Code-Globs. Mindestens ein Bereich, und jeder Bereich mindestens ein Muster. Anders als bei checkAreas ist ein leeres Muster-Array hier nicht erlaubt: Dort erfasst ein Bereich ohne Muster nichts und läuft nie, hier wäre er ein Bereich, den das Gate nie zuordnen kann.",
          "additionalProperties": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        },
        "testPattern": {
          "type": "string",
          "description": "Regulärer Ausdruck mit dem Platzhalter <ID>, der den Verweis auf eine Aussage im Testnamen findet. Fehlendes Feld = der Default.",
          "default": "\\[<ID>\\]"
        },
        "testGlobs": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Pfadmuster, unter denen nach den Tests gesucht wird."
        }
      },
      "required": [
        "seit",
        "bereiche"
      ],
      "additionalProperties": false
    },
    "mutationCommand": {
      "type": "string",
      "description": "Kommando für Mutations-Tests (optional). Leer-String oder fehlendes Feld = kein Mutations-Test. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": ""
    },
    "formatFixCommand": {
      "type": "string",
      "description": "Kommando, das Formatierungsverstöße mechanisch behebt (z.B. 'mvn spotless:apply' oder 'npx prettier --write .'). Nur der Nacht-Runner nutzt es: sind die buildChecks in der Salvage-Vorprüfung rot, läuft es genau einmal und die Checks werden genau einmal wiederholt, damit ein reiner Formatverstoss keinen ganzen Lauf beendet. Leer-String oder fehlendes Feld = kein Format-Fix. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": ""
    },
    "mainBranch": {
      "type": "string",
      "minLength": 1,
      "description": "Branch für lokale Commits und Push (Schritt 8). Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": "main"
    },
    "productionBranch": {
      "type": "string",
      "minLength": 1,
      "description": "Ziel-Branch für den PR in Schritt 9 (merge production). Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "default": "production"
    },
    "reviewScope": {
      "type": "string",
      "enum": [
        "diff",
        "full"
      ],
      "description": "Umfang des Review-Materials: 'diff' = git diff seit letztem Push; 'full' = gesamter Quelltext. Darf in workflow.config.local.json persönlich überschrieben werden.",
      "default": "diff"
    },
    "reviewModel": {
      "type": "string",
      "minLength": 1,
      "pattern": "^claude-",
      "description": "Die Claude-Variante des Reviewer-Paares reviewModel/reviewCommand: Modell-ID für den Reviewer-Subagent (Opus-Pin). Muss ein gültiger Claude-Modell-Identifier sein. Genau eines der beiden Felder ist gesetzt — eine fremde CLI gehört nach reviewCommand. Darf in workflow.config.local.json persönlich überschrieben werden.",
      "default": "claude-opus-4-8"
    },
    "reviewCommand": {
      "type": "string",
      "minLength": 1,
      "description": "Die Fremd-Variante des Reviewer-Paares reviewModel/reviewCommand: Kommandozeile einer fremden CLI (z.B. 'codex exec --model gpt-5'), die den Review-Prompt über stdin bekommt und ihre Antwort auf stdout schreibt. Genau eines der beiden Felder ist gesetzt; 'gesetzt' heißt, dass der Schlüssel vorhanden ist — ein Leer-String ist ungültig, nicht 'nicht gesetzt'. Darf in workflow.config.local.json persönlich überschrieben werden."
    },
    "triggers": {
      "type": "object",
      "description": "Trigger-Phrasen für die drei menschlichen Stop-Punkte. Darf in workflow.config.local.json persönlich überschrieben werden.",
      "properties": {
        "go": {
          "type": "string",
          "minLength": 1,
          "description": "Phrase für das GO zur Implementierung.",
          "default": "GO"
        },
        "push": {
          "type": "string",
          "minLength": 1,
          "description": "Phrase für Push auf mainBranch (Schritt 8).",
          "default": "push main"
        },
        "merge": {
          "type": "string",
          "minLength": 1,
          "description": "Phrase für PR nach productionBranch (Schritt 9).",
          "default": "merge production"
        }
      },
      "required": [],
      "additionalProperties": false,
      "default": {
        "go": "GO",
        "push": "push main",
        "merge": "merge production"
      }
    },
    "local": {
      "type": "object",
      "description": "Einstellungen für den lokalen Issue-Tracker (issueTracker: 'local'). Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "issuesDir": {
          "type": "string",
          "minLength": 1,
          "description": "Verzeichnis der Issue-Markdown-Dateien, relativ zum Projekt-Root.",
          "default": "issues"
        }
      },
      "required": [],
      "additionalProperties": false,
      "default": {
        "issuesDir": "issues"
      }
    },
    "columns": {
      "type": "object",
      "description": "Mapping der internen Status auf Board-Spalten- bzw. Label-Namen. Nur für den GitLab-Adapter relevant: 'done' ist dort immer der native Zustand Closed (unabhängig vom hier eingetragenen Namen). 'backlog' ist der native Zustand Open, wenn hier exakt \"Open\" eingetragen ist — sonst ein normales Label. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "backlog": {
          "type": "string",
          "minLength": 1,
          "default": "Backlog",
          "description": "GitLab-Sonderwert \"Open\": backlog wird als nativer Open-Zustand behandelt statt als Label."
        },
        "ready": {
          "type": "string",
          "minLength": 1,
          "default": "Ready",
          "description": "Name der Spalte bzw. des Labels für Ready — freigegeben, das GO des Menschen."
        },
        "in_progress": {
          "type": "string",
          "minLength": 1,
          "default": "In progress",
          "description": "Name der Spalte bzw. des Labels für In progress — das Arbeitspaket, an dem gerade gearbeitet wird."
        },
        "in_review": {
          "type": "string",
          "minLength": 1,
          "default": "In review",
          "description": "Name der Spalte bzw. des Labels für In review — lokal fertig, noch nicht gepusht."
        },
        "done": {
          "type": "string",
          "minLength": 1,
          "default": "Done",
          "description": "Nur Anzeigename für GitHub/lokal. GitLab behandelt done immer als nativen Closed-Zustand."
        }
      },
      "required": [],
      "additionalProperties": false,
      "default": {
        "backlog": "Backlog",
        "ready": "Ready",
        "in_progress": "In progress",
        "in_review": "In review",
        "done": "Done"
      }
    },
    "github": {
      "type": "object",
      "description": "GitHub-spezifische Einstellungen. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "projectNumber": {
          "type": "integer",
          "minimum": 1,
          "description": "Nummer des GitHub Project Boards (gh project list). Erforderlich für Board-Status-Operationen."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "toolbox": {
      "type": "object",
      "description": "Einstellungen für den Toolbox-Issue-Tracker (issueTracker: 'toolbox'). Privates Setup des Autors (eigenes Kanban-Tool), nicht Teil des Installer-Dialogs. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "host": {
          "type": "string",
          "minLength": 1,
          "description": "Basis-URL der Toolbox-Instanz."
        },
        "tokenFile": {
          "type": "string",
          "minLength": 1,
          "description": "Pfad (relativ zum Projektverzeichnis) zu einer Datei mit dem projekt-/board-gebundenen Token. Precedence: TBX_TOKEN-Umgebungsvariable > tokenFile > globaler tbx-Login (~/.config/toolbox-cli/tokens.json). Kein Klartext-Token in dieser Config — board.mjs bricht dann ab. Darf in workflow.config.local.json persönlich überschrieben werden."
        },
        "ideaStored": {
          "type": "boolean",
          "default": false,
          "description": "Lenkt neue Issues in den Ideen-Speicher des Boards statt direkt ins Backlog. true sendet kein 'direct' und legt als board-lose Idee im Pool an; false oder ein fehlender Wert sendet 'direct: true' und legt sofort mit Board-Nummer an — das ist die Vorgabe. Das früher gesendete Wire-Feld 'ideaStored' geht in keinem Modus mehr mit — der Server ignoriert es. Backends ohne 'direct' behalten ihr bisheriges Verhalten."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "issueReview": {
      "type": "object",
      "description": "Issue-Review über mehrere Modelle. Reviewer, die das Dokument nicht geschrieben haben, lesen es; wie viele je Stufe, sagt reviewStufen. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert. Die früheren Felder rounds und statusLabels sind seit Stufe 2 des Prozess-Umbaus entfallen und werden in einer Bestandsconfig ohne Fehler ignoriert.",
      "properties": {
        "requiredBeforeReady": {
          "type": "boolean",
          "default": false,
          "description": "Ist es true, stellt der Nacht-Runner Ready-Issues ohne Review-Marker kommentiert ins Backlog zurück. Default false, damit ein Kit-Update keinem Bestandsprojekt über Nacht den Runner anhält."
        },
        "reviewers": {
          "type": "array",
          "description": "Reihenfolge ist die Steuerung: Genommen werden die vordersten Einträge, die nicht der Autor sind; wie viele, sagt reviewStufen.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Kurzname, wird mit dem Autor-Modell des Issues verglichen."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "claude",
                  "command"
                ],
                "description": "'claude' läuft als Subagent über das Agent-Tool, 'command' als beliebiges fremdes CLI (Prompt über stdin)."
              },
              "model": {
                "type": "string",
                "description": "Nur bei kind 'claude': Modell-Identifier."
              },
              "command": {
                "type": "string",
                "description": "Nur bei kind 'command': Kommandozeile, z.B. 'codex exec --model gpt-5'."
              }
            },
            "required": [
              "name",
              "kind"
            ]
          }
        },
        "pairs": {
          "type": "object",
          "description": "Explizite Zuordnung Autor -> Reviewer. Steht der Autor hier, gewinnt sein Eintrag über die Reihenfolge-Regel. Ohne pairs wählt die Regel immer die vordersten Einträge — ein hinten stehendes fremdes Modell käme nie zum Zug. Ein Name, den es in reviewers nicht gibt, und ein Autor, der sich selbst nennt, sind harte Fehler.",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "reviewStufen": {
      "type": "object",
      "description": "Besetzung und Blickwinkel der drei Prüfstufen: das fachliche Anliegen, der Plan dorthin, das einzelne Arbeitspaket. Während issueReview beschreibt, WER überhaupt prüft, steht hier, wie viele und mit welchen Rollen je Stufe geprüft wird. 'rollen' muss genau 'reviewer' verschiedene, nicht leere Namen enthalten — sonst harter Fehler. Fehlt der gesamte Block, gilt für jede Stufe reviewer 2 mit den Rollen 'vollstaendigkeit-pruefbarkeit' und 'scope-risiko-bestand'; fehlt nur eine Stufe im vorhandenen Block, ist das ein Fehler. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "fachlich": {
          "type": "object",
          "description": "Prüfung des fachlichen Anliegens ([Fachlich]-Issue), bevor daraus ein Plan wird.",
          "properties": {
            "reviewer": {
              "type": "integer",
              "minimum": 1,
              "default": 2,
              "description": "Wie viele Reviewer diese Stufe prüfen."
            },
            "rollen": {
              "type": "array",
              "minItems": 1,
              "uniqueItems": true,
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "Ein Rollenname je Reviewer, in der Reihenfolge der Zuteilung."
            }
          },
          "required": [
            "reviewer",
            "rollen"
          ],
          "additionalProperties": false
        },
        "plan": {
          "type": "object",
          "description": "Prüfung des Plandokuments ([Plan]-Issue), bevor es in Arbeitspakete zerfällt.",
          "properties": {
            "reviewer": {
              "type": "integer",
              "minimum": 1,
              "default": 2,
              "description": "Wie viele Reviewer diese Stufe prüfen."
            },
            "rollen": {
              "type": "array",
              "minItems": 1,
              "uniqueItems": true,
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "Ein Rollenname je Reviewer, in der Reihenfolge der Zuteilung."
            }
          },
          "required": [
            "reviewer",
            "rollen"
          ],
          "additionalProperties": false
        },
        "issue": {
          "type": "object",
          "description": "Prüfung des einzelnen Arbeitspakets vor dem GO. Nur noch ein Reviewer: Was Form und Schnitt betrifft, ist auf den beiden Stufen davor bereits geprüft.",
          "properties": {
            "reviewer": {
              "type": "integer",
              "minimum": 1,
              "default": 1,
              "description": "Wie viele Reviewer diese Stufe prüfen."
            },
            "rollen": {
              "type": "array",
              "minItems": 1,
              "uniqueItems": true,
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "Ein Rollenname je Reviewer, in der Reihenfolge der Zuteilung."
            }
          },
          "required": [
            "reviewer",
            "rollen"
          ],
          "additionalProperties": false
        }
      },
      "required": [
        "fachlich",
        "plan",
        "issue"
      ],
      "additionalProperties": false
    },
    "night": {
      "type": "object",
      "description": "Der Nachtbetrieb. Die Nacht-Kette unter kette, die Liste erlaubter Modellnamen unter modelle. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "kette": {
          "type": "object",
          "description": "Budgets und Kennzeichen der Nacht-Kette (night.mjs --kette). Ein Fachplan mit dem Label geht abends hinein; jede Zahl ist ein Abbruchgrund mit Grund im Bericht, kein Fehler des Prozesses.",
          "properties": {
            "label": {
              "type": "string",
              "default": "kit:night",
              "description": "Das Kennzeichen am Fachplan, das die Kette startet. Jedes Setzen autorisiert genau eine Kette; der Start verbraucht es."
            },
            "varianteBLabel": {
              "type": "string",
              "default": "kit:durchziehen",
              "description": "Das Kennzeichen, das einen Fachplan für die Umsetzungsstufe der Kette (Variante B) kennzeichnet."
            },
            "planMin": {
              "type": "number",
              "exclusiveMinimum": 0,
              "default": 20,
              "description": "Zeitbudget der Stufe Plan in Minuten, einschliesslich Korrekturrunden."
            },
            "paketeMin": {
              "type": "number",
              "exclusiveMinimum": 0,
              "default": 15,
              "description": "Zeitbudget der Stufe Pakete in Minuten, einschliesslich Korrekturrunden."
            },
            "reviewMin": {
              "type": "number",
              "exclusiveMinimum": 0,
              "default": 15,
              "description": "Zeitbudget der Prüfer-Session am Plan in Minuten."
            },
            "abdeckungMin": {
              "type": "number",
              "exclusiveMinimum": 0,
              "default": 10,
              "description": "Zeitbudget der Abdeckungs-Session in Minuten, die die Pakete gegen den Fachplan hält."
            },
            "umsetzungMin": {
              "type": "number",
              "exclusiveMinimum": 0,
              "default": 120,
              "description": "Zeitbudget der Umsetzungsstufe (Variante B) in Minuten, über alle Implementierungs-Sessions der Kette."
            },
            "kostenUsd": {
              "type": "number",
              "exclusiveMinimum": 0,
              "default": 50,
              "description": "Kostenbudget je Kette in US-Dollar, summiert über alle Sessions der Kette; geprüft nach jeder Session."
            },
            "kostenUsdB": {
              "type": "number",
              "exclusiveMinimum": 0,
              "default": 150,
              "description": "Kostenbudget je Kette in US-Dollar für die Umsetzungsstufe (Variante B), summiert über alle Sessions der Kette; geprüft nach jeder Session."
            },
            "korrekturrunden": {
              "type": "integer",
              "minimum": 1,
              "default": 2,
              "description": "Höchstzahl der Korrektursessions je Dokument nach einer roten Formprüfung."
            }
          },
          "additionalProperties": false
        },
        "modelle": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "^claude-"
          },
          "description": "Die Modellnamen, die der Nacht-Runner starten darf — geordnet, absteigend nach Stärke: der erste Eintrag ist das stärkste, der letzte das schnellste Modell. Die Ordnung ist nicht Kosmetik: /issues leitet daraus ab, welches Modell es einem Arbeitspaket empfiehlt, und nachts fragt niemand nach. Das pattern ^claude- ist zugleich die Absicherung — ohne die Liste wanderte ein Wert aus einem Issue-Body unbesehen in argv, und ein Paket mit '--dangerously-skip-permissions' wäre ein Angriff über eine Karte. Fehlt das Feld oder ist die Liste leer, startet jede Session mit dem Modell des Laufs."
        },
        "stufen": {
          "type": "object",
          "description": "Modell oder Kommando je Schwierigkeitsstufe eines Arbeitspakets, geordnet schwer/mittel/leicht. Fehlt eine Stufe, weicht der Nachtlauf zur nächststärkeren aus, bis notfalls zum Modell des Laufs selbst. Wirkt nur im nächtlichen Lauf — tagsüber wählt der Mensch sein Modell selbst. Ein modell muss auch in night.modelle stehen (sonst Fehler bei der Konfigurationsprüfung).",
          "properties": {
            "schwer": {
              "type": "object",
              "description": "Modell oder Kommando für eine schwere Aufgabe. Genau eines der beiden Felder ist gesetzt.",
              "properties": {
                "modell": {
                  "type": "string",
                  "minLength": 1,
                  "pattern": "^claude-",
                  "description": "Modell-ID für diese Stufe. Muss auch in night.modelle stehen."
                },
                "kommando": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Kommandozeile eines fremden Programms für diese Stufe — ein Projekt-Artefakt derselben Vertrauensstufe wie reviewCommand, das pattern ^claude- gilt hier nicht."
                },
                "name": {
                  "type": "string",
                  "description": "Selbstauskunft des Programms neben kommando."
                }
              },
              "additionalProperties": false,
              "oneOf": [
                {
                  "required": [
                    "modell"
                  ],
                  "not": {
                    "required": [
                      "kommando"
                    ]
                  }
                },
                {
                  "required": [
                    "kommando"
                  ],
                  "not": {
                    "required": [
                      "modell"
                    ]
                  }
                }
              ]
            },
            "mittel": {
              "type": "object",
              "description": "Modell oder Kommando für eine mittelschwere Aufgabe. Genau eines der beiden Felder ist gesetzt.",
              "properties": {
                "modell": {
                  "type": "string",
                  "minLength": 1,
                  "pattern": "^claude-",
                  "description": "Modell-ID für diese Stufe. Muss auch in night.modelle stehen."
                },
                "kommando": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Kommandozeile eines fremden Programms für diese Stufe — ein Projekt-Artefakt derselben Vertrauensstufe wie reviewCommand, das pattern ^claude- gilt hier nicht."
                },
                "name": {
                  "type": "string",
                  "description": "Selbstauskunft des Programms neben kommando."
                }
              },
              "additionalProperties": false,
              "oneOf": [
                {
                  "required": [
                    "modell"
                  ],
                  "not": {
                    "required": [
                      "kommando"
                    ]
                  }
                },
                {
                  "required": [
                    "kommando"
                  ],
                  "not": {
                    "required": [
                      "modell"
                    ]
                  }
                }
              ]
            },
            "leicht": {
              "type": "object",
              "description": "Modell oder Kommando für eine leichte Aufgabe. Genau eines der beiden Felder ist gesetzt.",
              "properties": {
                "modell": {
                  "type": "string",
                  "minLength": 1,
                  "pattern": "^claude-",
                  "description": "Modell-ID für diese Stufe. Muss auch in night.modelle stehen."
                },
                "kommando": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Kommandozeile eines fremden Programms für diese Stufe — ein Projekt-Artefakt derselben Vertrauensstufe wie reviewCommand, das pattern ^claude- gilt hier nicht."
                },
                "name": {
                  "type": "string",
                  "description": "Selbstauskunft des Programms neben kommando."
                }
              },
              "additionalProperties": false,
              "oneOf": [
                {
                  "required": [
                    "modell"
                  ],
                  "not": {
                    "required": [
                      "kommando"
                    ]
                  }
                },
                {
                  "required": [
                    "kommando"
                  ],
                  "not": {
                    "required": [
                      "modell"
                    ]
                  }
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "stufenRegel": {
          "type": "string",
          "description": "Ersetzt die mitgelieferte Regel, nach der /issues und /task die Stufe eines Arbeitspakets bestimmen. Fehlt das Feld oder ist der Text leer, gilt die Regel des Kits."
        }
      },
      "additionalProperties": false
    },
    "aufwand": {
      "type": "object",
      "description": "Der Aufwand des Prozesses (unbeaufsichtigte Läufe): wie viele Ergebnisstände die Auswertung betrachtet und ab welchen Schwellen sie einen Befund meldet. Der Befund ist kein Gate, er hält keinen Lauf und kein Veröffentlichen auf. Optional — fehlt der Block oder ein Feld darin, gelten die eingebauten Vorgaben, damit ein bestehendes Projekt die Auswertung ohne weitere Einrichtung bekommt. Gilt teamweit; ein abweichender Wert in workflow.config.local.json wird ignoriert.",
      "properties": {
        "laeufe": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 10,
          "description": "Wie viele der jüngsten abgeschlossenen Ergebnisstände die Auswertung höchstens einbezieht, die jüngsten zuerst."
        },
        "schwellen": {
          "type": "object",
          "description": "Ab welchem Anteil die Auswertung einen Befund meldet.",
          "properties": {
            "pruefungAnteil": {
              "type": "number",
              "default": 0.5,
              "description": "Anteil, den die größte einzelne Pflichtprüfung an der gesamten Prüfzeit einnehmen darf, bevor ein Befund erscheint. Ein Wert zwischen 0 und 1."
            },
            "eingrenzungOhneWirkung": {
              "type": "boolean",
              "default": true,
              "description": "Ob ein Befund erscheint, wenn die Eingrenzung der Prüfungen über Bereiche gemessen, aber nie gegriffen hat."
            },
            "werkzeugAnteil": {
              "type": "number",
              "default": 0.5,
              "description": "Anteil, den reine Werkzeugarbeit an der gesamten Laufzeit einnehmen darf, bevor ein Befund erscheint. Ein Wert zwischen 0 und 1."
            },
            "schreibkostenAnteil": {
              "type": "number",
              "default": 0.75,
              "description": "Anteil, den die Kosten des dritten Postens (Schreiben) an den Gesamtkosten einnehmen dürfen, bevor ein Befund erscheint. Ein Wert zwischen 0 und 1."
            }
          },
          "required": [],
          "additionalProperties": false
        }
      },
      "required": [],
      "additionalProperties": false
    }
  },
  "required": [],
  "additionalProperties": false,
  "oneOf": [
    {
      "required": [
        "reviewModel"
      ],
      "not": {
        "required": [
          "reviewCommand"
        ]
      }
    },
    {
      "required": [
        "reviewCommand"
      ],
      "not": {
        "required": [
          "reviewModel"
        ]
      }
    }
  ],
  "defaults": {
    "codeHost": "github",
    "issueTracker": "github",
    "buildChecks": [],
    "mutationCommand": "",
    "mainBranch": "main",
    "productionBranch": "production",
    "reviewScope": "diff",
    "reviewModel": "claude-opus-4-8",
    "triggers": {
      "go": "GO",
      "push": "push main",
      "merge": "merge production"
    },
    "local": {
      "issuesDir": "issues"
    },
    "columns": {
      "backlog": "Backlog",
      "ready": "Ready",
      "in_progress": "In progress",
      "in_review": "In review",
      "done": "Done"
    }
  },
  "validationRules": [
    {
      "field": "codeHost",
      "rule": "enum",
      "allowed": [
        "github",
        "gitlab",
        "local"
      ],
      "error": "codeHost muss 'github', 'gitlab' oder 'local' sein."
    },
    {
      "field": "issueTracker",
      "rule": "enum",
      "allowed": [
        "github",
        "gitlab",
        "local",
        "toolbox"
      ],
      "error": "issueTracker muss 'github', 'gitlab', 'local' oder 'toolbox' sein."
    },
    {
      "field": "reviewScope",
      "rule": "enum",
      "allowed": [
        "diff",
        "full"
      ],
      "error": "reviewScope muss 'diff' oder 'full' sein."
    },
    {
      "field": "reviewModel",
      "rule": "pattern",
      "pattern": "^claude-",
      "error": "reviewModel muss eine Claude-Modell-ID sein (z.B. 'claude-opus-4-8')."
    },
    {
      "fields": [
        "reviewModel",
        "reviewCommand"
      ],
      "rule": "exactlyOne",
      "error": "Genau eines von reviewModel (Claude-Subagent) und reviewCommand (fremde CLI, Prompt über stdin) muss gesetzt sein."
    }
  ]
}
