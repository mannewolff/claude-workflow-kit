## Synthese, Runde 1

Interaktiver Lauf, ein Pruefer (Stufe `issue`). 9 Funde, alle uebernommen, keiner verworfen. Kein Fund der Klassen `gate` oder `alternativen`.

### Entscheidungen

- fable, "Akzeptanzkriterium 4 ist heute schon erfuellt, weil das Suchmuster am Umlaut vorbeigeht" (BLOCKER, `korrektur`) — uebernommen. **Am Bestand nachgeprueft:** Zeile 154 schreibt „ueber" mit Umlaut, mein Muster suchte die transliterierte Form und ergab im unveraenderten Repo bereits `0`. Das Kriterium haette nichts geprueft, und das Zitat in der Aufgabe haette niemanden zur Fundstelle gefuehrt. Neu: `.*ber` im Muster, Zitat auf den Bestand korrigiert.
- fable, "Zwei weitere Tests verlangen den alten Weg und sind nicht Teil der Aufgabe" (BLOCKER, `korrektur`) — uebernommen. **Am Bestand nachgeprueft:** `test/board-text-quelle.test.mjs` (Schleife ueber `review`, `implement-ready`, `implement-next`) und `test/board-create-bodyfile.test.mjs` verlangen `--text -` bzw. `--body -`. Aus zwei umzustellenden Testdateien werden vier; sonst waere „node --test ohne fehlgeschlagene Tests" mit der Aufgabe nicht erreichbar. Geprueft und ausgeschlossen: `test/board-offene-zweige.test.mjs` taucht in derselben Suche auf, prueft aber das Laufzeitverhalten von `--text -` bei nicht lesbarem stdin — diese Faehigkeit bleibt, der Test bleibt gueltig.
- fable, "In `implement-next` steht ein zweiter Satz, der dem neuen Weg widerspricht" (WICHTIG, `korrektur`) — uebernommen. **Am Bestand nachgeprueft:** Zeile 163 verbietet Hilfsdateien fuer den Abschlussbericht und verweist auf „den `--text -`-Aufruf oben", den es nach der Umstellung nicht mehr gibt. Wird fortgeschrieben statt geloescht: keine Hilfsdateien **im Projektverzeichnis**.
- fable, "Die Begruendungsabsaetze 'Der Body geht ueber stdin' bleiben stehen" (WICHTIG, `korrektur`) — uebernommen. Drei Absaetze in `fachplan` (65), `techplan` (211) und `issues` (163) begruenden den alten Weg. Sie werden fortgeschrieben wie der #270-Absatz in #583: Das Quoting-Argument gilt weiter, die Schlussfolgerung wechselt.
- fable, "Akzeptanzkriterium 1 ist fuer `techplan` heute schon erfuellt" (WICHTIG, `korrektur`) — uebernommen. Die Stelle ist mehrzeilig mit Backslash und endet auf `--body -` ohne Heredoc; das Heredoc-Muster trifft sie nie. Neu ein eigenes Kriterium auf `--body -(\s|$)`. Dieselbe Form hatte ich beim Schneiden bereits einmal uebersehen — sie ist der Grund, warum der Plan zunaechst von elf statt zwoelf Stellen ausging.
- fable, "Die Spec-Aussage behauptet fuer sieben Skills eine Ausfall-Form, die es dort nicht gibt" (WICHTIG, `korrektur`) — uebernommen. Die Ausfall-Form haengt am Runden-Anker von `reviewZustand`; ein Abschlussbericht, ein Review-Ergebnis und ein `issue create` tragen keinen. Neu ist Aufgabe 6 mit einem eigenen Fehlerpfad je Stelle, und die Spec-Zeile behauptet die Ausfall-Form nicht mehr.
- fable, "Das Zitat in Aufgabe 3 stammt aus `/issue-review`" (HINWEIS, `korrektur`) — uebernommen; die Fundstellen sind jetzt einzeln benannt statt pauschal zitiert.
- fable, "Akzeptanzkriterium 2 ist fuer zwei der sieben Dateien heute schon erfuellt" (HINWEIS, `korrektur`) — uebernommen. `fachplan` und `issues` enthalten `--body-file` bereits; das Kriterium ist jetzt an die Befehlsstelle gebunden und prueft zusaetzlich den Satz zum eigenen Werkzeugaufruf.
- fable, "`board.mjs` Zeile 27/28: der zu ersetzende Satz geht ueber zwei Zeilen" (HINWEIS, `korrektur`) — uebernommen. **Am Bestand nachgeprueft:** Die Begruendung „keine Datei, die jemand aufraeumen muss" steht in Zeile 28 und 2839 und widerspraeche dem neuen Weg, waere nur das Wort „bevorzugt" ersetzt worden.
- fable, "Was raus kann" — uebernommen: Kriterium 2 in der alten Form ersetzt, der erzaehlende Satz im Kontext gestrichen.

Uebernommen: 9 · Verworfen: 0
